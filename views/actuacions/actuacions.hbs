<h2>Actuacions</h2>

<ul>
  {{#each dbActuacions}}
    <li>
        {{this.diada}} creada per <img src="{{this.author.userImage}}" alt="foto de perfil">  {{this.author.nom}} 
        <br>
        <form action="/actuacions/{{this.id}}/assistir" method="POST"></form>
          <button type="submit">Hi vaig assistir</button>
        </form>
        <br>
        <a href="/actuacions/{{this._id}}">Detalls</a> | <a href="/actuacions/{{this._id}}/edit">Edita l'actuació</a>
        <form action="/actuacions/{{this.id}}/delete" method="POST">
          <button type="submit">Esborra l'actuacio</button>
        </form>
        <hr>
    </li>
  {{/each}}
</ul>